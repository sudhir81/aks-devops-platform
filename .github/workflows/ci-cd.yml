name: 🚀 AKS DevOps Platform CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  id-token: write   # ✅ Required for Azure OIDC login
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: sample-app
      CHART_PATH: charts/sample-app
      RELEASE_NAME: sample-app
      K8S_NAMESPACE: default
      ENV_PATH: envs/dev

    steps:
      # -----------------------------
      # Checkout Code
      # -----------------------------
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # -----------------------------
      # Validate Required Secrets
      # -----------------------------
      - name: 🧠 Validate Required Secrets
        run: |
          echo "🔍 Validating required secrets..."
          REQUIRED=("AZURE_CLIENT_ID" "AZURE_TENANT_ID" "AZURE_SUBSCRIPTION_ID" "ACR_NAME" "AKS_CLUSTER_NAME" "AKS_RESOURCE_GROUP")
          for VAR in "${REQUIRED[@]}"; do
            if [ -z "${{ secrets[VAR] }}" ]; then
              echo "❌ Missing secret: $VAR"
              exit 1
            fi
            echo "✅ $VAR found"
          done
          echo "✅ All required secrets verified."

      # -----------------------------
      # Azure Login via OIDC
      # -----------------------------
      - name: 🔐 Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------
      # Validate & Ensure Role Assignments (Self-Healing)
      # -----------------------------
      - name: 🔑 Validate Azure Role Assignments
        run: |
          echo "🔍 Validating AKS and Storage permissions for GitHub OIDC App..."
          SP_ID=${{ secrets.AZURE_CLIENT_ID }}
          SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID }}
          RG_NAME=${{ secrets.AKS_RESOURCE_GROUP }}
          AKS_NAME=${{ secrets.AKS_CLUSTER_NAME }}

          # Get AKS resource ID
          AKS_ID=$(az aks show --name "$AKS_NAME" --resource-group "$RG_NAME" --query id -o tsv)
          echo "🧩 AKS Resource ID: $AKS_ID"

          # ✅ Check AKS access
          ROLE_EXISTS=$(az role assignment list \
            --assignee "$SP_ID" \
            --scope "$AKS_ID" \
            --query "[?roleDefinitionName=='Azure Kubernetes Service Cluster User Role'] | length(@)" -o tsv)

          if [ "$ROLE_EXISTS" -eq 0 ]; then
            echo "⚠️ Missing AKS Cluster User Role — assigning..."
            az role assignment create \
              --assignee "$SP_ID" \
              --role "Azure Kubernetes Service Cluster User Role" \
              --scope "$AKS_ID"
            echo "✅ AKS role assignment added."
          else
            echo "✅ AKS role already assigned."
          fi

          # ✅ Check Terraform backend (Storage Blob Data Contributor)
          echo "🔍 Checking Terraform state storage permissions..."
          STORAGE_SCOPE=$(az storage account list --resource-group rg-landingzone-tfstate --query "[0].id" -o tsv)
          ROLE_EXISTS=$(az role assignment list \
            --assignee "$SP_ID" \
            --scope "$STORAGE_SCOPE" \
            --query "[?roleDefinitionName=='Storage Blob Data Contributor'] | length(@)" -o tsv)

          if [ "$ROLE_EXISTS" -eq 0 ]; then
            echo "⚠️ Missing Storage Blob Data Contributor — assigning..."
            az role assignment create \
              --assignee "$SP_ID" \
              --role "Storage Blob Data Contributor" \
              --scope "$STORAGE_SCOPE"
            echo "✅ Storage role assignment added."
          else
            echo "✅ Storage access already assigned."
          fi

      # -----------------------------
      # Setup Terraform
      # -----------------------------
      - name: 🧱 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.8.5

      # -----------------------------
      # Terraform Init / Plan
      # -----------------------------
      - name: ⚙️ Terraform Init
        run: terraform -chdir=${{ env.ENV_PATH }} init -input=false
        env:
          ARM_USE_OIDC: true
          ARM_SKIP_CLI_CREDENTIAL: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: 🧮 Terraform Plan
        run: terraform -chdir=${{ env.ENV_PATH }} plan -input=false -out=tfplan -var-file=dev.tfvars
        env:
          ARM_USE_OIDC: true
          ARM_SKIP_CLI_CREDENTIAL: true
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # -----------------------------
      # Build & Push Docker Image
      # -----------------------------
      - name: 🐳 Azure Container Registry Login
        run: az acr login --name ${{ secrets.ACR_NAME }}
          
      - name: 🏗️ Build and Push Docker Image
        run: |
          echo "🔧 Building Docker image..."
          docker build -t ${{ secrets.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }} .
          echo "📦 Pushing image to ACR..."
          docker push ${{ secrets.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}

      # -----------------------------
      # Connect to AKS
      # -----------------------------
      - name: 🎯 Get AKS Credentials
        env:
          AKS_CLUSTER_NAME: ${{ secrets.AKS_CLUSTER_NAME }}
          AKS_RESOURCE_GROUP: ${{ secrets.AKS_RESOURCE_GROUP }}
        run: |
          echo "🔐 Connecting to AKS..."
          echo "Using cluster: $AKS_CLUSTER_NAME"
          echo "Using resource group: $AKS_RESOURCE_GROUP"
          az aks get-credentials \
            --name "$AKS_CLUSTER_NAME" \
            --resource-group "$AKS_RESOURCE_GROUP" \
            --overwrite-existing

      # -----------------------------
      # Verify AKS Connection
      # -----------------------------
      - name: 🔍 Verify AKS Context
        run: |
          echo "🔹 Current context:"
          kubectl config current-context || echo "⚠️ No context found"
          echo "🔹 Checking AKS nodes:"
          kubectl get nodes || echo "⚠️ AKS not reachable"
      
      # -----------------------------
      # Helm Deploy
      # -----------------------------
      - name: ⛵ Deploy via Helm
        run: |
          echo "🚀 Deploying Helm release..."
          helm upgrade --install $RELEASE_NAME $CHART_PATH \
            --namespace $K8S_NAMESPACE \
            --set image.repository=${{ secrets.ACR_NAME }}.azurecr.io/${{ env.IMAGE_NAME }} \
            --set image.tag=latest \
            --wait
          echo "✅ Helm deployment complete!"

      # -----------------------------
      # ✅ Deployment Summary
      # -----------------------------
      - name: ✅ Deployment Summary
        run: |
          echo "Deployment complete!"
          echo "Fetching service details..."
          kubectl get svc -n $K8S_NAMESPACE
          
          echo "⏳ Waiting for external IP..."
          for i in {1..20}; do
            EXTERNAL_IP=$(kubectl get svc -n $K8S_NAMESPACE | grep $RELEASE_NAME | awk '{print $4}')
            if [[ "$EXTERNAL_IP" != "<pending>" && "$EXTERNAL_IP" != "" ]]; then
              echo "🌐 App URL: http://$EXTERNAL_IP"
              exit 0
            fi
            echo "Still waiting for external IP... ($i/20)"
            sleep 15
          done
          
          echo "⚠️ External IP not found yet — check AKS LoadBalancer status."
